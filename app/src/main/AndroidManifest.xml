<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidAppTemplate"
        tools:targetApi="35">

        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity" android:exported="false" />

        <activity android:name=".ui.usuario.CadastroUsuarioActivity" android:exported="false" />

        <activity android:name=".ui.dadospessoais.DadosPessoaisActivity" android:exported="false" />
        <activity android:name=".ui.contato.ContatoActivity" android:exported="false" />
        <activity android:name=".ui.curriculo.ExperienciaProfissionalActivity" android:exported="false" />
        <activity android:name=".ui.formacaoAcademica.FormacaoAcademicaActivity" android:exported="false" /> <activity android:name=".ui.curriculo.VisualizarCurriculoActivity" android:exported="false" />

    </application>
</manifest>